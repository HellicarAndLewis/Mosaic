#include <featurex/Comparator.h>

namespace fex {
  
  Comparator::Comparator(std::vector<Descriptor>& descriptors)
    :descriptors(descriptors)
  {

  }

  Comparator::~Comparator() {
  }

  void Comparator::match(Descriptor& input) {
    
  }

} /* namespace fex */
